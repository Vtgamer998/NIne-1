# NINE-1: dependencies com versoes minimas para seguranca
# Uso local (Termux Android):
#   pkg install python && pip install torch --index-url https://download.pytorch.org/whl/cpu

# PyTorch com suporte a FlashAttention e RoPE
torch>=2.1.0,<3.0.0

# Numpy para processamento de dados
numpy>=1.24,<2.0.0
